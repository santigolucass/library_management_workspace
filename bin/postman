#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$ROOT_DIR"

exec docker compose \
  -f docker-compose.base.yml \
  -f docker-compose.postman.yml \
  up --build --abort-on-container-exit --exit-code-from postman
